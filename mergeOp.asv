function p=mergeOp(o,i)
%function has the similar work with the mergeElement function.
len=length(o);
if i==1
    for j=1:len-1
        o(j)=o(j+1);
    end
else
    for j=i:len-1
        o(j)=o(j+1);
    end
end
o(len)=[];
p=o;
     